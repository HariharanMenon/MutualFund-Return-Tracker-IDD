# ============================================================
# Root Environment Variables Template
# Copy to .env and populate values (never commit .env)
# ============================================================

# Backend URL used by the frontend (set in Render dashboard or locally)
# VITE_API_URL=http://localhost:8000

# Python environment (optional — Render reads runtime.txt)
# PYTHON_VERSION=3.11.7
