# ============================================================
# Backend Python Dependencies (pinned versions)
# Install: pip install -r requirements.txt
# ============================================================

# Web framework
fastapi==0.104.1
uvicorn==0.24.0

# File upload support
python-multipart==0.0.6

# Excel parsing
# NOTE: Spec listed openpyxl==3.11.0 which does not exist as a release.
# [Inferred Implementation] Using openpyxl==3.1.2 (latest stable 3.x).
openpyxl==3.1.2
pandas==2.1.4

# XIRR / financial calculation
numpy_financial==1.0.0

# Data validation
pydantic==2.5.3
pydantic-settings==2.1.0

# ============================================================
# Testing (not installed in production)
# [Inferred Implementation] Required for backend/tests/ suite.
# ============================================================
pytest==7.4.4
pytest-asyncio==0.23.2
httpx==0.26.0
